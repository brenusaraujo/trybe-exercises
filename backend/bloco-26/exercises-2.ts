type bird = {
  beak: string;
  wingSize: number;
  whiteColor: boolean;
};

function sum(x: number, y: number): number {
  return x + y;
}

type address = {
  street: string;
  number: number;
  zipCode: number;
  closeToDowntown: boolean;
};
